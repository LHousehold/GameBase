DEFINE ACCESS OVERWRITE player ON DATABASE TYPE RECORD
	SIGNIN ( SELECT * FROM player WHERE record::id(id) = $playerId AND playerSecret = $playerSecret)
	DURATION FOR TOKEN 1h, FOR SESSION 12h;

ALTER TABLE match PERMISSIONS FOR SELECT WHERE $auth.id IN playersIds;
ALTER TABLE player PERMISSIONS FOR SELECT WHERE $auth.id = id;